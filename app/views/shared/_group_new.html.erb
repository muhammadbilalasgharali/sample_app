<%= form_for(@group) do |f| %>
<div class="field">
  <%= f.text_area :name , placeholder: "Enter Name", required: true %>
  <%= f.text_area :description , placeholder: "Enter Description", required: true %>
</div><%= f.submit "Post", class: "btn btn-primary" %>
<% end %>
