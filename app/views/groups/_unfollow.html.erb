<%= form_for(current_user.new_relationships.find_by(group_id: @group.id), method: :delete ,remote: true) do |f| %>
<%= hidden_field_tag :group_id, @group.id %>
<%= f.submit "Unfollow", class: "btn" %>
<% end %>
